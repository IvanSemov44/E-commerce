/**
 * WishlistCard Types
 * Type definitions for the WishlistCard component
 */

export interface WishlistCardProps {
  productId: string;
  productName: string;
  price: number;
  image?: string;
}
