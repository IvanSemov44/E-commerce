<?xml version="1.0" encoding="utf-8"?>
<RunSettings>
  <RunConfiguration>
    <!-- Use all available logical CPUs for faster test execution. -->
    <!-- Keep integration tests isolated (factory/db per test/class) to avoid contention. -->
    <MaxCpus>0</MaxCpus>
  </RunConfiguration>
</RunSettings>
